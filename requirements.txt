numpy>=1.26
scipy>=1.12,<2
xlsxwriter>=3.2

python-docx>=1.1
Pillow>=10.0

openpyxl>=3.1

# v0.2.46+ updater uses verified HTTPS + Windows system trust + certifi CA bundle + SHA-256.
# No third-party cloud SDK is required for updates.

# Native desktop shell (WebView2 on Windows; browser fallback remains available).
pywebview==6.2.1

# TLS trust bundle for updater; Windows system trust is merged at runtime.
certifi>=2024.8.30
